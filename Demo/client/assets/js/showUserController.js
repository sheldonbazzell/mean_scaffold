app.controller('showUserController',['$scope','$routeParams', 'UsersFactory', 
function($scope,$routeParams,UsersFactory ) {
 UsersFactory.show($scope.user, $routeParams.id)
 		$scope.show(function(data){  
 				$scope.user = data
 		})
 	}
}])
app.controller('showUserController',['$scope','$routeParams', 'UsersFactory', 
function($scope,$routeParams,UsersFactory ) {
 UsersFactory.show($scope.user, $routeParams.id)
 		$scope.show(function(data){  
 				$scope.user = data
 		})
 	}
}])
app.controller('showUserController',['$scope','$routeParams', 'UsersFactory', 
function($scope,$routeParams,UsersFactory ) {
 UsersFactory.show($scope.user, $routeParams.id)
 		$scope.show(function(data){  
 				$scope.user = data
 		})
 	}
}])
app.controller('showUserController',['$scope','$routeParams', 'UsersFactory', 
function($scope,$routeParams,UsersFactory ) {
 UsersFactory.show($scope.user, $routeParams.id)
 		$scope.show(function(data){  
 				$scope.user = data
 		})
 	}
}])
